Cogitave builds the foundational, AI-native platform that agents run on — from
scratch, vertically integrated, and in the open. Replace this prose with your
own. It lives here, separate from the structured `index.page.yaml`, so the data
stays clean and the copy stays easy to review and translate.
